{
    "scope": "vscode",
    "rules": [],
    "conditions": {
        "state": "language == 'python'"
    },
    "commands": [
        [
            "new function",
            "`def ():` {left}_3"
        ],
        ["import", "import spaceForward()"],
        ["for iterable", "`for  in `clipboard.get(): {left}_length(clipboard.get()) {left}_5"],
        ["for enumerate", "`for i,  in enumerate(` clipboard.get() `):` {left}_length(clipboard.get()) {left}_16"]
    ],
    "functions": []
}